# WebSocket

A basic chat app built using the WebSocket API.

To start the WebSocker server, cd into server/ and run `npm start`

To start a development server, an option is to use the Live Server extension and launch it from index.html
